<h1> My Invoices </h1>
<section class="merchant-invoices">
  <% @merchant.distinct_invoices.each do |invoice| %>
    <div class="invoice-<%= invoice.id %>">
      <p>Invoice #<%= link_to invoice.id, merchant_invoice_path(@merchant, invoice) %></p>
    </div>
  <% end %>
</section>
